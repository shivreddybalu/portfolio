# Netlify configuration for Angular app
[build]
  command = "npm run build"
  publish = "dist/shiv-app/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
